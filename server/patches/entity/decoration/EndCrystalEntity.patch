--- a/net/minecraft/entity/decoration/EndCrystalEntity.java
+++ b/net/minecraft/entity/decoration/EndCrystalEntity.java
@@ -19,6 +19,7 @@
 import net.minecraft.util.math.BlockPos;
 import net.minecraft.world.World;
 import net.minecraft.world.explosion.Explosion;
+import org.loomdev.api.event.block.BlockIgniteEvent;
 
 public class EndCrystalEntity extends Entity {
 
@@ -52,7 +53,13 @@
             BlockPos blockpos = this.getBlockPos();
 
             if (((ServerWorld) this.world).getEnderDragonFight() != null && this.world.getBlockState(blockpos).isAir()) {
+                // Loom start :: fire BlockIgnitedEvent
+                if (org.loomdev.loom.event.LoomEventDispatcher.onBlockIgnite(this.world, blockpos, this, BlockIgniteEvent.Cause.END_CRYSTAL).isCancelled()) {
+                    return;
+                }
+
                 this.world.setBlockState(blockpos, AbstractFireBlock.getState(this.world, blockpos));
+                // Loom end
             }
         }
 
